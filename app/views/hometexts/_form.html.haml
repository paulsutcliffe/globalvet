= form_for @hometext do |f|
  - if @hometext.errors.any?
    #error_explanation
      %h2= "#{pluralize(@hometext.errors.count, "error")} prohibited this hometext from being saved:"
      %ul
        - @hometext.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label 'Título'
    = f.text_field :title
  .field
    = f.label 'Contenido'
    = f.text_area :content
  .field
    = f.label 'Link'
    = f.text_field :link
  .actions
    = f.submit 'Guardar'
