= form_for @reino do |f|
  - if @reino.errors.any?
    #error_explanation
      %h2= "#{pluralize(@reino.errors.count, "error")} prohibited this reino from being saved:"
      %ul
        - @reino.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :nombre
    = f.text_field :nombre
  .field
    = f.label :imagen
    = f.text_field :imagen
  .actions
    = f.submit 'Save'
