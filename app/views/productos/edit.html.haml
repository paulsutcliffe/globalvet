#page-content
  %h1.page-title
    = page_title! 'Editando producto'
    
  .content
    = render 'form'

    = link_to 'Ver', reino_categoria_subcategoria_tipo_producto_path(@producto.tipo.subcategoria.categoria.reino, @producto.tipo.subcategoria.categoria, @producto.tipo.subcategoria, @producto.tipo, @producto), class: 'button-back-cms'
    = link_to 'Regresar', reino_categoria_subcategoria_tipo_productos_path(@producto.tipo.subcategoria.categoria.reino, @producto.tipo.subcategoria.categoria, @producto.tipo.subcategoria, @producto.tipo), class: 'button-back-cms'
